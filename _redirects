# Cloudflare Pages. El acceso vive en `app.html` (raíz), así que se sirve en
# comando.pro/app SIN barra final: Pages redirige con 308 cualquier carpeta a
# su forma con barra, y eso no se puede desactivar desde aquí. Por eso la
# página está fuera de `app/`, que sigue guardando el panel y los assets.
/app/  /app  301
